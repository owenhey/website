export interface PortfolioContent{
    title: string,
    content: string[],
    date: string,
    images: string[]
}

export interface FlagAnswerData{
    countryName : string;
    imageUrl : string;
    area: string;
}